<h1>Edit the student named <%= @student.name %> here</h1>

<%= form_for @student do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :mod %>
  <%= f.number_field :mod %>

  <%= f.submit %>

<% end %>
